Django>=4.2.4					# El framework web de Python
psycopg2-binary>=2.9.6			# Para conectarse a la base de datos PostgreSQL
gunicorn>=20.1.0				# Para servir la aplicación en producción
qrcode>=7.3.1					# Para generar códigos QR
pillow>=10.0.0					# Para generar códigos QR
requests==2.31.0				# Para hacer peticiones HTTP (API REST)
python-dotenv==1.0.1			# Para cargar variables de entorno desde un archivo .env
django-cors-headers>=4.0.0		# Para permitir peticiones CORS
PyJWT>=2.6.0					# Para generar tokens JWT
pyotp==2.8.0					# Para generar códigos OTP (One Time Password)
django-jazzmin==2.6.0           # Para tener un panel de administración de Django más elegante
django-ninja==1.3.0             # Crear documentación interactiva de la API
daphne>=4.0.0
channels>=4.0.0
channels-redis>=4.1.0
django-redis>=4.12.0
asgiref>=3.7.0
twisted[tls,http2]>=21.7.0      # Para soporte TLS y HTTP/2 en Daphne/Channels
djangorestframework>=3.14.0